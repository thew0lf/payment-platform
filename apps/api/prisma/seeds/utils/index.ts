/**
 * Utility Seeds Index
 * Exports utility scripts for seed management
 */

// Note: These are standalone scripts, not imported functions
// Run directly via: npx ts-node prisma/seeds/utils/reset-demo.ts
// Run directly via: npx ts-node prisma/seeds/utils/seed-entity-codes.ts
